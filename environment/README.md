# Environment

The canonical local development environment uses Python 3.11. Environment lock
files will be generated from `pyproject.toml` after dependency resolution.

Definitive training requires an NVIDIA environment whose GPU model, driver,
CUDA, cuDNN, PyTorch, MONAI, and operating-system metadata are recorded in
every run artifact.
