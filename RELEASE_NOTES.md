# Release notes

## v2.0.0 — prepared, not released

- Preserves the complete legacy study under
  `v1-bounded-2d-component-study`.
- Adds the BraTS-Africa TCIA v1 integrity and zero-overlap audit.
- Freezes deterministic five-fold development membership and an equal
  five-seed model matrix.
- Adds topology-preserving parameter- and compute-matched RES controls.
- Adds term-level loss/Methods parity metadata, 2.5D context extraction, and
  official nnU-Net v2/MONAI baseline provenance.
- Adds exact-version local environment provenance, Docker/Apptainer audit
  definitions, backend-neutral resource fields, and explicit execution guards.
- Selects Apache-2.0 for original repository code.

Scientific v2 results, weights, DOI and the final manuscript are not released:
the full 615-run compute allocation is unresolved and the external
confirmatory set has not been opened for inference.
